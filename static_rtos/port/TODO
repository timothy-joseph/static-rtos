might need to restructure this so it includes either:
a directory for each port with 2 source files inside: context_port, timer_port
or
2 directories: one for context ports and another for timer ports

